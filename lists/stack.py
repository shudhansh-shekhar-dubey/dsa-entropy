from lists.linkedlist import Node, List


class StackLL:
    def __init__(self):
        self.list = List(None)
        self.top = self.firstNode

    def push(self, node: Node):
      firstNode = self.firstNode
      node.
      self.firstNode = 